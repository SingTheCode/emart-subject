import styled from "styled-components";

const ProductList = styled.div`
  height: auto;
  padding-top: 48px;
  overflow: hidden;
`;

const Target = styled.div`
  height: 1px;
`;

export default {
  ProductList,
  Target,
};
